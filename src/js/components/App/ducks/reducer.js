import {

} from './actions';

const initialState = {
    //TODO
}

function AppReducer(state = initialState, action) {
    switch(action.type) {
        default:
            return state;
    }
}

export default AppReducer;